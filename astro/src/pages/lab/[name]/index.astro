---
import LabLayout from "../../../layouts/LabLayout.astro";
import { labPagesMeta } from "../../../data/labPagesMeta";

const allContent = import.meta.glob("../../../lab-content/*/index.html", {
  query: "?raw",
  import: "default",
  eager: true,
});

export function getStaticPaths() {
  const paths = import.meta.glob("../../../lab-content/*/index.html", {
    query: "?raw",
    import: "default",
    eager: true,
  });
  return Object.keys(paths).map((path) => {
    const name = path.split("/").at(-2) ?? "";
    return { params: { name } };
  });
}

const { name } = Astro.params;
const meta = labPagesMeta[name];
const content = allContent[`../../../lab-content/${name}/index.html`] ?? "";
---

<LabLayout
  title={meta.title}
  description={meta.description}
  lang={meta.lang}
  noindex={meta.noindex}
  headExtra={meta.headExtra}
  {content}
/>
