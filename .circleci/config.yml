version: 2
jobs:
  publish:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run: ls
      - run: scp index.html bokken.io:~/

workflows:
  version: 2
  deploy:
    jobs:
      - publish:
          filters:
            branches:
              only: master
