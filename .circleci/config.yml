version: 2
jobs:
  publish:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run: ls
      - run: scp index.html bokken.io:~/

workflows:
  version: 2
  deploy:
    jobs:
      - publish:
          filters:
            branches:
              only: master
